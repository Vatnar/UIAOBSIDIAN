Middleware
MQTT, CORBA, DDS.

MQTT
Broker, that runs somewhere, that is connected to a network.
MQTT Clientz which a module communicates too. The mqtt client talks to the mqtt broker.
The bus is the mqtt clients and broker. 

We also have DDS which is brokerless.
