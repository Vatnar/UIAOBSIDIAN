Recover from faults:
Reintroduction 
- Shadow: operating a previously failed or in-service upgraded component in a "shadow mode" for a predefined time prior to reverting the component back to an active role.
- State resynchronization: Partner to active redundancy and passive redundancy where state information is sent from active to standby components.
- Escalating restart: recover from faults by varying the granularity of the component(s) restarted and minimizeing the level of service affected.
- Non-stop forwarding: functionality is split into supervisory and data. If a supervisor fails, a router continues forwarding packets along known routes while protocol information is recovered and validated.
- Removal from service: Temporarily placing a system component in an out of service state for the purpose of mitigating potetntial system failures.
- Transactions: bundling state updates so that asynchronousmessages exchanged between distributed components are atomic, consistent, isolated, and durable. 
- Predictive model: monitor the state of health of a process to ensure that the system is opperating within nominal parameters, take corrective action when conditionsa redetected that are predictive of likely future faults.
- Exception prevention: preventing system exceptions from occuring by masking a fault, or preventing it via smart pointers, abstract data types, wrappers. 
- Increase Competence set: designing a component to handle more cases - faults - as part of its normal operation. Basically weaken the precondition

Patterns:
- 
- TMR Pattern for Av
	- Tripple Modular Redundancy (Voting)
	- Same inputs, and votes, majority wins.
	- Could be different decision rules also, and weight them. or average them.
	- Benefit: Simple to understand and to implement. 
	- Tradefoff: Increasing the level of replication, which raises the cost, and resulting the availibity, in systems with TMR, the statistical likelihood of two or more omopnenets failing is vanishingly small, and three componenets, reporesents a sweet spot between availability and cost.
- Forward Error correcting: SECDED ECC
	- Scenario: A high energy cosmic ray causes a single bit flip, in the ram of an asil d electric power steering controller
	- The mechanmism: the momory utilizes secded (single error correction, doublle error detection) ecc)
	- Th eFER action: instead of crashing the thread or halting the cpu to reload the data from a bacakup partition the integrated ecc hardware dynamically cporrects the flipped bit on the fly as the data is read, the cpu receives the rectified corrected value and seamlessly advances to the next instructions.
	- Using a previously correct value isnstead of dcurrent wrong value. Seamlessly swaps the failed physical sensor input for te calculated virtual sensor estimation. The system recalculates its path using the estimated value, preserving nominal steering capability within the fault tolerant time interval (FTTI).