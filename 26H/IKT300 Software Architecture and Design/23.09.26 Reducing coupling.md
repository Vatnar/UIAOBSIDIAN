interfaces, possible to mock, different 
packes so you dont have to recompile.
### reduce coupling:

- use intermediary.
- pubsub
- dynamic lookup service
- restrict dependencies
- refactor: underatekn when two modules are affected by the same change because they are partially duplicates of each other
- abstract common services, implment services just once in a more general form.
- encapsulate by interface.
- dependent on interface instead of implementation.
use a wrapper
- not a big difference between a wrapper and a interface, but often wrapper is done by a class wrapped another class.
- raise the abstraction level.
intermediary vs wrapper:
- Wrapper wraps the entire thing
- intermediary, only changes one side.