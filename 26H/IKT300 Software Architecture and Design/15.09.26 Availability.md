"It works on my machine"
- Property of software that is there and ready to carry out its task when you need it to be. 
- System to mark or repair faults such that they do not become failures.
- Builds on reliability by adding recovery. Minimize service outage time by mitigating faults.

Faults can be
- prevented,
- tolerated,
- removed,
- forecast.

We are concerned with
- How faults are detected,
- how frequent are they,
- what happens when they occur,
- how long a system may be out of operation,
- how faults or failures can be prevented,
- what notifications are require when a failure occurs.

Measure:
- MTBF: Mean time between failures
- MTTR: Mean time to repair
- Availability caluclated as (MTBF)/(MTBF + MTTR)

Source: Where does it come from  
Stimulus: Something is missing  
Artifact: What failed  
Environment: How it may behave when its already in a failure state  

Reactions:
- Log fault
- Notify entities
- Recover
- Disable source of events
- Temporarily unavailable
- Fix or mask the fault
- Operate in degraded mode, while repair
- Tim einterval when the system must be available
- Time to detect fault
- Time to repair
- Time interval which system can be in degraded
- Proportion or rate of certain class of results that are preventable or handles without failing.

General Byzantine algorithm. Used in blockchain today.
Voting is related to distributed systems.

Recover from faults:
- Reduntant spare. Duplicates.
	Hot spare, warm spare, and cold spare patterns. Differ in up-to-dateness. And how long it takes to come back up and running.
	- Active redundancy (hot spare), all nodes in a protection group receive and process identical inputs in paralell, allowing reduntant spare(s) to maintain synchronous state with the active node(s).
		- Because the redundant spare posesses an identical state to the active processor, it can take over from a failed componennt in a matter of milliseconds. 
		- The problem here is state synchronization.
	- Passive redundancy (warm spare).
		- Periodic checkpoint data
		-  Only active members input traffic. One of the duties is to provide redundant spare(s) with periodic state updates.
		- Loosely coupled, you can get the system in a safe state to synchronize. 
		- Less avilability but cheaper.
		HDFS:
			Hadoop distributed file system
			Breaks files into chunks, each chunk is atleast 3 copies on different servers.
			It has a NameNode that keeps track of what chunks are where.
			The NameNode has a warm spare. 
			It uses a map reduce algorithm
			If one data node goes down (misses heartbeats), it copies the chunk so it has 3 copies again.
			If one is "lagging", then start another node with the same job that has the same chunks.
			7/10 reqs are about availability.
	- Reduntant Node (Cold-spare)
		- No synchronization
		- For hardware.
		- Takes time, but you have the hardware.
		- High mean time to repair (MTTR), so bad for hihg avilability reqs.
		- Cheaper and less complex.
- Exception Handling: dealing with the exception by reporting it or handling it, potentially masking the fault by correcting the cause of the exception and retrying.
- Rollback: revert to a previous known good state, referred to as the "rollback line".
- Software Upgrade: in-service upgrades to executable code images in a non-service-affecting.
	- Monkey patching, doing it while its running (dangerous)
	- Use reflection, load stuff during runtime. (also risky)
- Retry: where a failure is transient retrying the operation may lead to success.
- Ignore faulty behaviour: ignoring messages sent from a source when it is determined that those messages are spurious.
- Graceful Degradation: maintains the most critical system functions in the presence of component failures, dropping less critical functions.
- Reconfiguration: reassigning responsibilities to the resources left functioning, while maintaining as much functionlity as possible.
- Shadow: 
Design into our systems how to recover from faults.
